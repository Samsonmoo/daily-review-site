### 完成內容

## engPCCU
* 完成靜態輸出與 GitHub Pages 發布設定，保留既有版本歷史並將新版首頁上線。
* 補齊正式網址、結構化資料、robots 與 sitemap 等 SEO/GEO 訊號，並驗證公開頁面可讀取。
* 修正資源卡片滑入與鍵盤焦點狀態的右側邊框，重新建置並發布。
* 產出可供 CMS 使用的完整 HTML 與 CSS 版本。

## verbatim
* 建立本機音訊轉錄、模型辨識與校訂流程。
* 完成 11 份含時間碼的逐字稿成品，涵蓋高精度稿、校訂稿與精修稿。
* 交叉複核部分逐字稿，校正專有名詞並保留無法確認片段的標記。

### 今日建議

## engPCCU
* 更新已過時的預覽骨架測試，避免失敗測試掩蓋真正回歸。
* 確認 CMS 版輸出是否需要納入版本控制，並清理未追蹤的開發檔案。

## verbatim
* 抽樣回聽不確定片段與重要專有名詞，完成交付前品質檢查。
* 將來源音檔、中間稿、成品與大型模型分層管理，避免誤納入版本控制。
* 補上批次轉錄與驗證說明，讓後續音檔可沿用同一流程。
